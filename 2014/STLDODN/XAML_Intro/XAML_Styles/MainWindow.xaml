<Window x:Class="XAML_Styles.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.Resources>

        </Grid.Resources>
       
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Button 
			Grid.Row="0"
            Content="This is a test"
			Click="Button_Click"
            >

            <Button.Resources>
                <Style TargetType="Button">
                    
                   
                </Style>
            </Button.Resources>
            
        </Button>

        <Button 
			Grid.Row="1"
            Content="Ok"
            />
        <Button 
			Grid.Row="2"
            Content="Ok"
           />

        <Button 
			Grid.Row="3"
            Content="Ok"
		   Style="{StaticResource extraWideRedButton}"
           />
    </Grid>
</Window>
